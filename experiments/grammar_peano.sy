(synth-fun lemma ((x Int) (y Int) (zero Int)) Bool

           ((Start Bool))

           ((Start Bool (
                   (= (plus x zero) x)
                   (= (plus x (succ y)) (succ (plus x y)))
                   (nat x)
                   (nat y)
                   (=> Start Start))))

)

(synth-fun rswitch () Int

           ((Start Int))

           ((Start Int (0)))

)